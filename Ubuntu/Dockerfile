# Use the official Ubuntu base image
FROM ubuntu:latest

# Update and install necessary tools
RUN apt-get update && apt-get install -y dnsutils iproute2 net-tools

# Set the default command to run an interactive shell
CMD ["/bin/bash"]

